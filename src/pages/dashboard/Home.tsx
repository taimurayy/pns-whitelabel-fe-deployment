import { Box } from "@mui/material";

import Layout from "../../components/Layout";

const Home: React.FC = () => {
  return (
    <Layout>
      <Box></Box>
    </Layout>
  );
};

export default Home;
